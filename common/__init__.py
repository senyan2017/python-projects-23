"""Shared helpers for the small command-line projects in this repo.

Only the genuinely duplicated bits live here (JSON file storage and the
menu loop). Each project keeps its own domain objects and display code so
it stays easy to read on its own.
"""
